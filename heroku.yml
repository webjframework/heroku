build:
languages:
   - java
run:
  web: bash -c "mkdir target/tomcat.$PORT; mkdir target/tomcat.$PORT/webapps; cp deployments/*.ini target/tomcat.$PORT/webapps; chmod a+x deployments/startup;  deployments/startup $PORT "
